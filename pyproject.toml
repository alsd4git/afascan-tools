[project]
name = "afascan-tools"
version = "0.1.0"
description = "OCR, normalization and local dashboards for AfaScan reports"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
